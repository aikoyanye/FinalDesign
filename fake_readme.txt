用了python3.6、tornado、pymysql、pyecharts、xlrd、xlwt、可能还有更多库

还有mysql数据库，数据库的信息写在main.py，在命令行中输入python3 main.py即可开启服务

系统的文件只有admin是有用的，其他都是没用的（因为不可抗力）

最好不要去修改js文件，因为写的很乱，本身我自己都不想看，所以不建议修改，功能都是正常的

数据库结构在根目录，final_design.sql

127.0.0.1:8080/admin